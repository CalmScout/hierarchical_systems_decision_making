В фале GenerateIPCM.py функции генерации матриц парных сравнений (четкие и интервальные)

Сильно согласованные матрицы парных сравнений
def createConsistantPCM (str):  
Расчет матрицы парных сравнений по одной строке, в которой один элемент сравнивают со всеми (включая себя) предложен Ротштейном А.П.

Метод генерирует случайным образом строку парных сравнений размерностью n 
def genRandLinePC(n): 

Слабо согласованные матрицы 
def createWeakConsistantPCM (str):
Формирует четкую слабо согласованную матрицу парных сравнений 

Формирует из четкой матрицы парных сравнений интервальную
Хорошо работает при формировании согласованной интервальной матрицы парных сравнений
Для слабо согласованных, часто формирует согласованную интервальную матрицу. Так как для интервальных матриц транзитивность только порядковая 
def createConsistantIPCM(PCM):

checkConsistensyIPCM.py модуль проверки на согласованность интервальных  матриц парных сравнений 

def isConsistentIPCM(L,U): проверяет на согласованность по определению 

isWeakConsistICPM1(L,U): проверяет на слабую согласованность по предпочтению по центру масс

def isWeakConsistICPM2(L,U):  проверяет на слабую согласованность по порядковому предпочтению
